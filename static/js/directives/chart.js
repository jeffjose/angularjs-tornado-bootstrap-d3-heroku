// Generated by CoffeeScript 1.7.1
module.directive("chart", function($parse) {
  return {
    restrict: "E",
    replace: false,
    scope: {
      data: "=chartData",
      width: "=width",
      height: "=height"
    },
    link: function(scope, element, attrs) {
      var chart, draw;
      chart = d3.select(element[0]).append("svg");
      draw = function() {
        var bars;
        chart.attr("height", scope.height).attr("width", scope.width);
        bars = chart.selectAll("rect").data(angular.fromJson(scope.data)).attr("width", function(d, i) {
          return d * 20;
        }).attr("height", 10).attr("x", 5).attr("y", function(d, i) {
          return i * 20;
        }).attr("fill", "steelblue");
        bars.enter().append("rect").attr("width", function(d, i) {
          return d * 20;
        }).attr("height", 10).attr("x", 5).attr("y", function(d, i) {
          return i * 20;
        }).attr("fill", "steelblue");
        bars.exit().remove();
      };
      scope.$watch("data", function(newVal, oldVal) {
        try {
          JSON.parse(newVal);
          draw();
        } catch (_error) {}
      });
    }
  };
});
